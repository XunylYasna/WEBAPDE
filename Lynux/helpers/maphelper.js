var map = function(hbs) {
    var helpers = {
        json: function(content) {
            return JSON.stringify(content);
        }
    }
}

module.exports.map = map;
module.exports.helpers = map(null); 